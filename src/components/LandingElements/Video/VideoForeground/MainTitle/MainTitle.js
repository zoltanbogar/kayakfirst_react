import React from 'react';

import classes from './MainTitle.css';

const mainTitle = () => (
    <div className={classes.MainTitle}>
        Universal paddle machine
    </div>
);

export default mainTitle;